# Folder: /site/src/lib/agentic/agents

1. **地位**: Agent 定义层，存放所有使用 OpenAI Agents SDK 构建的 Agent
2. **边界**: 接收原始输入 / 调用 LLM / 输出 Pydantic 模型实例 / 不处理 IO 或持久化
3. **约束**: 每个 Agent 必须使用 `output_type` 强制 JSON Schema 输出

## 成员清单

| 文件 | 角色 | 职责 |
|------|------|------|
| segmentation_agent.py | Segmentation Agent | 将文章按意图切分，输出 ArticleSegmentation |

---
**触发器**: 一旦本文件夹增删文件或架构逻辑调整，请立即重写此文档。
