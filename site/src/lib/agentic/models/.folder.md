# Folder: /site/src/lib/agentic/models

1. **地位**: 数据模型层，定义所有 Agent 的输入/输出结构
2. **边界**: 仅定义 Pydantic 模型 / 不包含业务逻辑 / 不依赖外部服务
3. **约束**: 所有模型必须继承 `pydantic.BaseModel`，使用 `Field` 添加描述

## 成员清单

| 文件 | 角色 | 职责 |
|------|------|------|
| segmentation.py | Segmentation Models | Intent, ArticleSegmentation 模型定义 |

---
**触发器**: 一旦本文件夹增删文件或架构逻辑调整，请立即重写此文档。
