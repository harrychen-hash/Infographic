# Folder: /site/src/lib/agentic/prompts

1. **地位**: Agent 的 prompt 模板存储目录，与代码逻辑分离
2. **边界**: 接收 Markdown 格式的 prompt 文件 / 通过 `load_prompt()` 输出字符串 / 不包含代码逻辑
3. **约束**: 文件名必须与 agent 名称对应（如 `chart_agent.md` 对应 `chart_agent.py`）

## 成员清单
| 文件 | 角色 | 职责 |
|------|------|------|
| chart_agent.md | Prompt Template | 图表选择 agent 的系统指令 |

---
**触发器**: 一旦本文件夹增删文件或架构逻辑调整，请立即重写此文档。
